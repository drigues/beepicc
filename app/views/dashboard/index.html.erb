<div class="min-h-screen bg-gray-100 flex items-center justify-center px-4">
  <div class="bg-white shadow-md rounded-lg p-8 w-full max-w-lg">
    <h1 class="text-2xl font-bold text-gray-800 mb-4">
      Welcome, <%= current_user.name %> 👋
    </h1>

    <p class="text-gray-600 mb-6">
      Your directory: <strong><%= current_user.directory %></strong>
    </p>

    <%= link_to "Log out", destroy_user_session_path, method: :delete, data: { turbo_method: :delete }, class: "inline-block px-4 py-2 bg-red-600 text-white rounded hover:bg-red-700" %>
  
  </div>
</div>
