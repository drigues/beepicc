<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= @profile.name %></title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-white text-gray-900 flex flex-col items-center p-6">
  <div class="max-w-lg w-full text-center">
    <h1 class="text-2xl font-bold mb-4"><%= @profile.name %></h1>
    <p class="text-sm text-gray-500 mb-6"><%= @profile.bio %></p>

    <% if @links.any? %>
      <ul class="space-y-2">
        <% @links.each do |link| %>
          <li>
            <a href="<%= link.url %>" target="_blank" class="block bg-gray-200 hover:bg-gray-300 rounded px-4 py-3 transition font-medium">
              <%= link.title %>
            </a>
          </li>
        <% end %>
      </ul>
    <% else %>
      <p class="text-gray-400 mt-6">No links yet.</p>
    <% end %>
  </div>
</body>
</html>
