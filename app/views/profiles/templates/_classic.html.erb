<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title><%= @profile.name %> | Beepi.cc</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100 text-center flex flex-col items-center justify-start min-h-screen p-4">
  <div class="max-w-md w-full">
    <% if @profile.avatar.attached? %>
      <%= image_tag @profile.avatar, class: "rounded-full w-28 h-28 object-cover mx-auto mb-4" %>
    <% end %>

    <h1 class="text-2xl font-bold"><%= @profile.name %></h1>
    <p class="text-gray-600 mb-6"><%= @profile.bio %></p>

    <% @links.each do |link| %>
      <a href="<%= link.url %>" target="_blank" class="block w-full bg-white border border-gray-300 rounded-lg p-4 mb-3 shadow hover:bg-gray-50 transition">
        <%= link.title %>
      </a>
    <% end %>
  </div>
</body>
</html>
